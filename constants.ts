export const MAX_FRAMES = 20; // Limit frames to avoid payload issues
export const CAPTURE_INTERVAL_MS = 2000; // Capture every 2 seconds
export const IMAGE_QUALITY = 0.6; // JPEG quality for screenshots

export const PLACEHOLDER_CODE = `# Waiting for analysis...
# Your generated automation script will appear here.`;
